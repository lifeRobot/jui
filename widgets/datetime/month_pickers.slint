import { NumberPickers } from "./number_pickers.slint";

export component MonthPickers inherits NumberPickers {
    min_limit: { enable:true, num:1 };
    max_limit: { enable:true, num:12 };
    num: 1;
}
